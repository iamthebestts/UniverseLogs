# Migrações: app roda na subida (RUN_MIGRATE=true). Ver docs/deploy.md.
NAME=logs-api
TYPE=site
MAIN=src/app.ts
START=bun run start
RAM=512
VERSION=latest

ID=logs-api

BUILD=bun install --frozen-lockfile